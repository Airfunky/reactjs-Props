function Header() {
  return (
    <header
      style={{
        background: "#333",
        color: "#fff",
        padding: "20px",
        textAlign: "center",
      }}
    >
      <h1>Product List</h1>
    </header>
  );
}

export default Header;