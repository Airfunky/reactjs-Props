function footer({text}) {
    return (
        <div style={{backgroundColor: "#ddae24",
            padding: "15px",
        }}>
            <p style={{color: "black",}}>{text}</p>
        </div>
    )
}

export default footer;